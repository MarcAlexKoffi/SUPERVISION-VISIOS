{
  "scripts": {
    "build:front": "cd FRONT && npm run build",
    "deploy": "npm run build:front && npx firebase deploy --only hosting"
  },
  "dependencies": {
    "bcrypt": "^6.0.0",
    "jsonwebtoken": "^9.0.3",
    "jspdf": "^4.2.0",
    "jspdf-autotable": "^5.0.7",
    "mysql2": "^3.17.4"
  },
  "devDependencies": {
    "@types/bcrypt": "^6.0.0",
    "@types/jsonwebtoken": "^9.0.10",
    "firebase-tools": "^15.8.0"
  }
}
